import React from 'react';
import { EndSession } from '../../components';

const EndSessionPage = () => {
    return (
        <div>
            <EndSession/>
        </div>
    );
};

export default EndSessionPage;