import React from 'react';
import {IntervalSettings} from '../../components';

const SynchroneIntervalSettingsPage = () => {
    return (
        <div>
            <IntervalSettings key={2} typeOfTraining={'synchrone'}/>
        </div>
    );
};

export default SynchroneIntervalSettingsPage;