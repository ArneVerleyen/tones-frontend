import React from 'react';
import { EndSessionChords } from '../../components';

const EndSessionChordsPage = () => {
    return (
        <EndSessionChords/>
    );
};

export default EndSessionChordsPage;